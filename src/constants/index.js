import { DB, LIMITS, LISTS, PINO_HTTP_OPTIONS, SERVER } from './constants.js';
import { MESSAGES } from './messages.js';
import { regex } from './regex.js';

const CONSTANTS = {
  MESSAGES,
  regex,
  SERVER,
  PINO_HTTP_OPTIONS,
  LISTS,
  DB,
  LIMITS,
};

export default CONSTANTS;
